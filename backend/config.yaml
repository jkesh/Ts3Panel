app:
  jwt_secret: "ts3PanelJWTSecretToken"
  port: ":8080"

ts3:
  protocol: "webquery"  # "webquery" | "tcp" | "ssh"
  host: "127.0.0.1"
  port: 10080           # webquery: 10080(http)/10443(https), tcp:10011, ssh:10022
  server_id: 1
  user: "serveradmin"   # tcp/ssh 使用
  password: ""          # tcp/ssh 使用
  api_key: ""           # webquery 必填
  https: false          # webquery + TLS 时设为 true
  base_path: ""         # 可选: 例如 "query"

# 新增数据库配置
database:
  driver: "postgres"      # "postgres" 或 "sqlite"
  host: "127.0.0.1"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "ts3panel"
  sslmode: "disable"
  timezone: "Asia/Shanghai"
